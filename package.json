{
  "scripts": {
    "server": "node .build/app.js",
    "start": "npm i && rm -rf .build/ && tsc && npm run server",
    "server:debug": "rm -rf .build/ && tsc && node --trace-warnings .build/app.js",
    "debug": "npm run server:debug"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "ioredis": "^5.6.1",
    "mongoose": "^8.15.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.18",
    "@types/express": "^5.0.1",
    "@types/node": "^22.15.18"
  }
}
